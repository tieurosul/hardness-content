<?php
namespace hardness;

function callback($array, $params = null) {
    // seu código
    // $array['campoAtual']
    // $array['todosCampos']['UsuarioGerouNF.C007_Nome']
    // $array['totais']['Nome_Total']

    if (empty($array['campoAtual'])) {
        $sql = "SELECT C007_Nome 
                FROM T162 
                    LEFT JOIN C007 ON C007_Id = T162_C007_Id 
                WHERE T162_Registro_Id = '{$array['todosCampos']['T007_Id']}'
                    AND T162_Acao = 'I'
                    AND T162_Tabela = 'T007'";
        $res = mysql_fetch_assoc(mysqli_query($sql));
        return $res['C007_Nome'];
    } else {
        return $array['campoAtual'];
    }
}