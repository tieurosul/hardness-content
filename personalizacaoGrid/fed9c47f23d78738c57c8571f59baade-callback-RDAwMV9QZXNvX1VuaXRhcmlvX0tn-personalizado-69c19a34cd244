<?php
namespace hardness;

function callback($array, $params = null) {
    // seu código
    // $array['campoAtual']
    // $array['todosCampos']['D001_Peso_Unitario_Kg']
    // $array['totais']['Nome_Total']
    if (!empty($array['campoAtual'])) {
        echo <<<EOT
        	<div class="divHidden"><script type="text/javascript"> if (typeof jQuery != 'undefined') {
                $("td[onclick*='D001_Peso_Unitario_Kg']").removeAttr("onclick");
	    } </script></div>
EOT;
    }
    return $array['campoAtual'];
}